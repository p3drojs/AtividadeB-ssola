
services:
  db:
    image: postgres:16
    container_name: postgres_db
    environment:
      POSTGRES_USER: pedro
      POSTGRES_PASSWORD: pedro
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: